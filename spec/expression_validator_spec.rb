require 'spec_helper'

describe ExpressionValidator do
  it 'has a version number' do
    expect(ExpressionValidator::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
